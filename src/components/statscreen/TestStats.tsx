import Screen from '@/components/Screen';
import { AppText } from '../texts/AppText';

export default function TestStats() {
  return (
    <Screen internal>
      <AppText>Simulado</AppText>
    </Screen>
  );
}
