export const assetsMap: Record<string, any> = {
  // This dictionary is generated on build
  // Each of its lines is of the form:
  // "'<JLPTLevel (N5, N4,...)><file path as listed on .db>': require('path/to/file')"
};
